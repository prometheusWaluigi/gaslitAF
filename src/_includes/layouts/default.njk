<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title }}</title>
  {% if pathPrefix %}
  <base href="{{ pathPrefix }}">
  {% endif %}
  <link rel="stylesheet" href="{{ pathPrefix }}styles.css">
</head>
<body>
  <header>
    <h1>{{ title }}</h1>
  </header>
  <main>
    {{ content | safe }}
  </main>
  <footer>
    <p>&copy; {{ "now" | date("YYYY") }} GASLIT-AF</p>
  </footer>
</body>
</html>
